# cmd/redirect の公開URL(短縮リンクのプレビュー表示に使用)。
# docker compose使用時はdocker-compose.yaml側でリバースプロキシの
# 公開URL(http://knives.localhost:${PROXY_PORT})に上書きされる。
# ここの値は素のpnpm devで動かす開発フロー用。
VITE_REDIRECT_BASE_URL=http://localhost:8081
